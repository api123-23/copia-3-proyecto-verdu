<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

## Current Task Summary (pinned by opencode)
- **Objective:** Terminar la PWA offline-first de informes técnicos (Air Power S.A.): navegación/creación offline, service worker con precache de build, instalable desde el celular, y (en curso) logo + pulso de UI móvil.
- **Logo:** El usuario adjuntó la imagen `/Users/ignacioricciutti/Downloads/WhatsApp Image 2026-08-28 at 11.00.10.jpg` (1024x1024). Copiada a `public/icons/logo-origen.jpg` (fuente reproducible). Reemplaza al rayo SVG anterior: `tags/scripts/generar-iconos.mjs` ahora genera icon-180/192/512 desde esa imagen (`sharp`, fit cover → PNG; verificado). El modelo NO puede ver la imagen: falta validación visual humana.
- **Íconos/PWA:** manifest apunta a `/icons/icon-192.png` (any), `/icons/icon-512.png` (any + maskable), `apple-touch-icon` = icon-180. `layout.tsx` meta iOS + viewportFit cover + theme #003e7a.
- **Ubicaciones del logo en la app:** login (`src/app/login/page.tsx`, 80px rounded-2xl centrado), cabecera fija de lista (`page.tsx`, w-7) y editor (`EditorInforme.tsx`, w-7, oculto en <sm), barra "AIR POWER S.A." de ambas vistas (w-5). Componente `src/components/LogoTipo.tsx` con `img` a `/icons/icon-192.png`.
- **UI polish (móvil):** feedback táctil `active:scale-*` en cabeceras/ENVIAR/NUEVO/Crear informe/tarjetas/Actualizar; íconos add/send en botones; estado vacío del listado con logo; safe-area-inferior (`env(safe-area-inset-bottom)` + 3rem) en lista/editor/login; `overscroll-behavior-y: none` y `-webkit-tap-highlight-color: transparent` en `globals.css` (CSS de base, no prólogo `@layer` para touch-action). Build OK (0 errores TS), lint 0 errores (mismos 5 warnings previos).
- **Pendiente:** deploy (regenerar `public/sw.js` del precache ya hecho por build), ejecutar `supabase/schema.sql` en Supabase, y verificación visual del logo en celular (instalación PWA).
- **Fix regresiones (commiteado por el usuario en `38d59eb` "solucion 999 errores (capaz)")**:
  - `parsearRuta` (`hashRuta.ts:28`): el regex `^/informe/(.+)$` capturaba `/informe/nuevo` antes que el chequeo de ruta "nuevo" → el botón NUEVO/"Crear informe" abría un editor con id `nuevo` y fallaba ("No se pudo cargar el informe") en PC y celular. Ahora `/informe/nuevo` se chequea primero.
  - Se re-crearon las rutas legadas `src/app/informe/nuevo/page.tsx` y `src/app/informe/[id]/page.tsx` como shims client que redirigen a `/#/informe/nuevo` y `/#/informe/<id>` (evitan 404 en enlaces viejos / shell viejo del SW en el celular).
  - `generate-sw.mjs`: install tolerante (`Promise.allSettled`, un asset que falle no rompe el SW, `skipWaiting` siempre) y estáticos con stale-while-revalidate (cache-first + revalidate en background) en vez de `cache.addAll`.
  - Verificado: build OK (rutas `/`, `/login`, `/informe/nuevo`, `/informe/[id]`), lint 0 errores, `next start` → todos los 39 recursos del precache responden 200, rutas 200.
- **Fix error GE + opciones compresor (commiteado por el usuario en `9f40125` "fix de campos2...")**:
  - Error al guardar GE: "violates check constraint informes_grupo_electrogeno_ge_funcionamiento_carga_altern_check" = valores legacy `si`/`no` contra dominio Ok/Mal. Falta reejecutar `supabase/schema.sql`.
  - `schema.sql`: helper idempotente `public.dropar_checks_de_columna(tabla, columna)` (dropea TODOS los checks de la columna, usa `pg_constraint` + `unnest(conkey)`, tolera nombres truncados por renombres) reemplaza los `drop constraint if exists` por nombre. Migraciones nuevas: `estado_bateria` de motocompresor a Ok/Mal; compresor `aceite_unidad` si/no → ok/bajo/alto (si→ok, no→bajo) y `tiempo_y_delta` → ok/bajo/alto (si→ok, no/mal→bajo); GE sigue con carga_alternador/inspeccion_bateria a Ok/Mal, perdidas_aceite a Si/No, aceite/agua con 'alto', amperaje numérico. Create table compresor: `aceite_unidad` y `tiempo_y_delta` ya con checks Ok/Bajo/Alto.
  - `src/lib/informes.ts`: `normalizarValores(tipo, valores)` aplica conversión legacy (GE: carga_alternador/inspeccion_bateria si/no→ok/mal, perdidas_aceite optimo/bajo/alto→no/si, mal→bajo en niveles; compresor: aceite_unidad y tiempo_y_delta si/no/mal→ok/bajo). Se llama en `construirAnexa`, `cargarAnexa` y `cargarAnexaGE` (cubre sync y editor). `remoto.ts` normaliza también al traer del servidor (import desde `informes`, NO `sync`).
  - `types.ts`: tipo `OkBajoAlto`; `ValoresBase.inst_electrica` y `carroceria` de `SiNo` a `OkMal`; `InformeCompresor` saca `aceite_unidad` del Omit y la sobreescribe con `OkBajoAlto`.
  - `SeccionValores.tsx`: `aceite_unidad` del compresor usa `OPCIONES_NIVEL` (Ok/Bajo/Alto, `esCompresor` ya definido) y motocompresor conserva `OPCIONES_SI_NO`; `tiempo_y_delta` de `OPCIONES_OK_BAJO` a `OPCIONES_NIVEL`.
- **Numeración de informes → 100% Supabase (aplicado, sin commitear):** se reemplazó tabla contador+trigger (`contador_informes`/`asignar_numero_informe()`/`trg_asignar_numero`) por una secuencia real `public.numero_informe_seq` con `default nextval(...)` en `numero_registro` (líneas 298-319 de `supabase/schema.sql`), asignada por orden de llegada, atómica ante syncs paralelos; `setval` idempotente a `max(numero_registro)`. El cliente sigue mostrando "№ —" hasta sincronizar (offset en `sync.ts:271`/`remoto.ts:14`). NO cambió el orden de la lista (sigue por `fecha_hora`).
- **Íconos inline (aplicado, sin commitear):** los íconos eran glifos de la fuente web externa "Material Symbols Outlined" (`fonts.googleapis.com`, `display=optional`, excluida del SW por cross-origin) → no renderizaban hasta recargar. Ahora componente `src/components/Icono.tsx` con SVGs inline (`@material-symbols/svg-400@0.47.0`, apachev2, viewBox "0 -960 960 960", `fill="currentColor"`) para add, arrow_back, send, calendar_today, location_on, close, add_a_photo. Se quitaron el `<link>` de `layout.tsx` y la clase `.material-symbols-outlined` de `globals.css`; los 7 usos fueron reemplazados en `page.tsx`, `ListaInformes.tsx`, `EditorInforme.tsx`, `secciones.tsx`, `SeccionFotos.tsx`.
  - Verificado: build OK (39 recursos precacheados), lint 0 errores (ahora 4 warnings: desapareció el de `no-page-custom-font`). Pendiente: reejecutar `schema.sql` en Supabase (nueva numeración + migraciones GE/compresor pendientes + garantizar cascade), deploy, y probar en celular.
- **Integridad DELETE CASCADE (aplicado, sin commitear):** el usuario detectó que borrar filas de `informes_generales` no borraba las de las tablas hijas. Causa: `create table if not exists` no agrega/repara FKs en tablas preexistentes (versiones viejas sin `on delete cascade`). Fix en `supabase/schema.sql` (líneas 161-199): función idempotente `public.garantizar_cascade(tabla)` que (1) borra huérfanos (hijas sin padre), (2) dropea cualquier FK hacia `informes_generales`, (3) re-crea con `on delete cascade`, aplicada a motocompresor/compresor/vehiculos/grupo_electrogeno/informe_archivos. No toca RLS ni las políticas (todas `using (true)`). `dropar_checks_de_columna` solo afecta constraints CHECK, nunca FKs.

<!-- END -->
